# model checkpoint name meaning: 
# f"{MODEL_NAME}_{LR}_{BATCH_SIZE}_{LATENT_SIZE}_{BETA}.pth"
MODEL:
  TYPE: "CGAN"
  NAME: "baseline_CGAN" # base name used to save the model
  CHECKPOINT_FOLDER: "./CGAN/checkpoints"
  ARCHITECTURE_YAML_NAME: baseline.yaml
  RESUME_FROM_CHECKPOINT: True

TRAINING:
  NUM_EPOCHS: 1000
  BATCH_SIZE: 512
  NUM_WORKERS: 10
  LABEL_SMOOTHING: 0.1
  WITH_AUGMENTATION: False # aug
  GENERATOR:
    LR: 0.001 # 1e-3
  DISCRIMINATOR: 
    LR: 0.001 # 1e-3


TEST_ONLY: False

DATASET_PATH: "/mnt/datasets/eeg/celeba/" # MATTIA DATASET PATH

OUTPUT_PATH: "CGAN/Results"
